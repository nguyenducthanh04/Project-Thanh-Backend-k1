<h1>Danh sach student</h1>
<table class="table table-bordered">
  <thead>
    <tr>
      <th width="5%">STT</th>
      <th>Tên</th>
      <th>Email</th>
      <th>Phone</th>
      <th>Address</th>
    </tr>
  </thead>
  <tbody>
    <% listStudent.forEach((item, index) => { %>
    <tr>
      <td><%= index + 1 %></td>
      <td>
        <a href="/teacher/classDetail/<%= item.id %>"><%= item.User.name %></a>
      </td>
      <td><%= item.User.email %></td>
      <td><%= item.User.phone %></td>
      <td><%= item.User.address %></td>
    </tr>
    <% }) %>
  </tbody>
</table>
